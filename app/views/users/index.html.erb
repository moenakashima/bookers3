<div class="container">
  <div class="row px-5 px-sm-0">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= render 'users/info', user: current_user %>
      <h2>New book</h2>
      <%= render 'books/form', book: @book %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <span><%= link_to 'グループを作成する', groups_path, method: :post %></span>|
      <span><%= link_to 'グループ一覧' groups_path %></span>
      <%= render 'index', users: @users %>
        </table>

    </div>

  </div>
</div>
